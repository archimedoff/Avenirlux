# Google OAuth — AvenirLux

## Callback URLs (Google Cloud Console)

**Authorized redirect URIs:**

- `http://localhost:3000/api/auth/callback/google`
- `https://avenirlux.com/api/auth/callback/google`

**Authorized JavaScript origins:**

- `http://localhost:3000`
- `https://avenirlux.com`

## Environment variables

See `.env.example`. Production: set the same keys in Vercel (never commit secrets).
